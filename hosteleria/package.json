{
  "name": "hosteleria-tpv",
  "version": "2.0.0",
  "description": "TPV completo para hostelería — LAN + Electron",
  "main": "electron/main.js",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "start":    "node --experimental-sqlite server.js",
    "dev":      "node --experimental-sqlite --watch server.js",
    "electron": "electron . --experimental-sqlite"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer":  "^1.4.5-lts.2"
  },
  "devDependencies": {
    "electron": "^30.0.0"
  }
}
